diff --git a/simpleble/src/backends/windows/PeripheralBase.cpp b/simpleble/src/backends/windows/PeripheralBase.cpp
index 0edb984..65749f8 100644
--- a/simpleble/src/backends/windows/PeripheralBase.cpp
+++ b/simpleble/src/backends/windows/PeripheralBase.cpp
@@ -17,6 +17,7 @@
 #include "winrt/base.h"
 
 #include <iostream>
+#include <thread>
 
 using namespace SimpleBLE;
 using namespace std::chrono_literals;
@@ -33,8 +34,22 @@ PeripheralBase::PeripheralBase(advertising_data_t advertising_data) {
 }
 
 PeripheralBase::~PeripheralBase() {
+    callback_on_connected_.unload();
+    callback_on_disconnected_.unload();
+    _cleanup_gatt();
     if (connection_status_changed_token_ && device_ != nullptr) {
-        device_.ConnectionStatusChanged(connection_status_changed_token_);
+        try {
+            device_.ConnectionStatusChanged(connection_status_changed_token_);
+        } catch (...) {
+        }
+        connection_status_changed_token_ = {};
+    }
+    if (device_ != nullptr) {
+        try {
+            device_.Close();
+        } catch (...) {
+        }
+        device_ = nullptr;
     }
 }
 
@@ -72,13 +87,37 @@ void PeripheralBase::update_advertising_data(advertising_data_t advertising_data
 }
 
 void PeripheralBase::connect() {
-    device_ = async_get(BluetoothLEDevice::FromBluetoothAddressAsync(_str_to_mac_address(address_)));
+    const auto bluetooth_address = _str_to_mac_address(address_);
+    if (address_type_ == SimpleBLE::BluetoothAddressType::RANDOM) {
+        device_ = async_get(BluetoothLEDevice::FromBluetoothAddressAsync(
+            bluetooth_address, winrt::Windows::Devices::Bluetooth::BluetoothAddressType::Random), 30000);
+    } else if (address_type_ == SimpleBLE::BluetoothAddressType::PUBLIC) {
+        device_ = async_get(BluetoothLEDevice::FromBluetoothAddressAsync(
+            bluetooth_address, winrt::Windows::Devices::Bluetooth::BluetoothAddressType::Public), 30000);
+    } else {
+        device_ = async_get(BluetoothLEDevice::FromBluetoothAddressAsync(bluetooth_address), 30000);
+    }
 
     // Attempt to connect to the device.
+    std::string last_error;
+    bool discovered = false;
     for (size_t i = 0; i < 3; i++) {
-        if (_attempt_connect()) {
-            break;
+        try {
+            if (_attempt_connect()) {
+                discovered = true;
+                break;
+            }
+        } catch (const std::exception& err) {
+            last_error = err.what();
         }
+        // WinRT can report ConnectionStatus::Connected before the remote GATT
+        // database is ready. Immediate retries repeat the same Unreachable result.
+        if (i + 1 < 3) std::this_thread::sleep_for(1000ms);
+    }
+
+    if (!discovered) {
+        const std::string detail = last_error.empty() ? "no usable GATT service was returned" : last_error;
+        throw SimpleBLE::Exception::OperationFailed("GATT discovery failed after 3 attempts: " + detail);
     }
 
     if (is_connected()) {
@@ -86,8 +125,10 @@ void PeripheralBase::connect() {
             [this](const BluetoothLEDevice device, const auto args) {
                 if (device.ConnectionStatus() == BluetoothConnectionStatus::Disconnected) {
                     this->disconnection_cv_.notify_all();
-
-                    SAFE_CALLBACK_CALL(this->callback_on_disconnected_);
+                    try {
+                        SAFE_CALLBACK_CALL(this->callback_on_disconnected_);
+                    } catch (...) {
+                    }
                 }
             });
 
@@ -96,18 +137,18 @@ void PeripheralBase::connect() {
 }
 
 void PeripheralBase::disconnect() {
-    gatt_map_.clear();
+    _cleanup_gatt();
     if (device_ != nullptr) {
-        device_.Close();
-
-        std::unique_lock<std::mutex> lock(disconnection_mutex_);
-        if (disconnection_cv_.wait_for(lock, 10s, [=] { return !this->is_connected(); })) {
-            // Disconnection successful
-        } else {
-            SIMPLEBLE_LOG_ERROR("Disconnection failed");
-            throw SimpleBLE::Exception::OperationFailed("Disconnection attempt was not acknowledged.");
+        // Revoke the event before Close(): WinRT can queue Disconnected after this
+        // object (and the Node callback userdata) has already been destroyed.
+        if (connection_status_changed_token_) {
+            try {
+                device_.ConnectionStatusChanged(connection_status_changed_token_);
+            } catch (...) {
+            }
+            connection_status_changed_token_ = {};
         }
-
+        device_.Close();
         device_ = nullptr;
     }
 }
@@ -316,9 +357,13 @@ void PeripheralBase::_subscribe(BluetoothUUID const& service, BluetoothUUID cons
 
     gatt_characteristic_holder.value_changed_callback = [=](const GattCharacteristic& sender,
                                                             const GattValueChangedEventArgs& args) {
-        // Convert the payload to a ByteArray.
-        ByteArray payload = ibuffer_to_bytearray(args.CharacteristicValue());
-        callback(payload);
+        // No exception may escape a WinRT event handler: Windows terminates the
+        // process with a fail-fast code when a late callback throws during teardown.
+        try {
+            ByteArray payload = ibuffer_to_bytearray(args.CharacteristicValue());
+            callback(payload);
+        } catch (...) {
+        }
     };
 
     // Register the callback.
@@ -334,73 +379,143 @@ void PeripheralBase::_subscribe(BluetoothUUID const& service, BluetoothUUID cons
     }
 }
 
-bool PeripheralBase::_attempt_connect() {
+void PeripheralBase::_cleanup_gatt() noexcept {
+    for (auto& [service_uuid, service] : gatt_map_) {
+        for (auto& [characteristic_uuid, characteristic] : service.characteristics) {
+            if (characteristic.value_changed_token && characteristic.obj != nullptr) {
+                try {
+                    characteristic.obj.ValueChanged(characteristic.value_changed_token);
+                } catch (...) {
+                }
+                characteristic.value_changed_token = {};
+            }
+            characteristic.value_changed_callback = nullptr;
+            characteristic.descriptors.clear();
+        }
+        service.characteristics.clear();
+        if (service.obj != nullptr) {
+            try {
+                service.obj.Session().MaintainConnection(false);
+            } catch (...) {
+            }
+            try {
+                service.obj.Close();
+            } catch (...) {
+            }
+            service.obj = nullptr;
+        }
+    }
     gatt_map_.clear();
+    mtu_ = 0;
+}
+
+bool PeripheralBase::_attempt_connect() {
+    _cleanup_gatt();
+
+    static const std::string kProvisioningService = "00001827-0000-1000-8000-00805f9b34fb";
+    static const std::string kProxyService = "00001828-0000-1000-8000-00805f9b34fb";
+    std::string requested_service;
+    // A transition advertisement can briefly contain both values. Proxy is the
+    // newest state and must win, matching Klixa's advertisement parser.
+    for (const auto& candidate : {kProxyService, kProvisioningService}) {
+        if (service_data_.count(candidate) != 0) {
+            requested_service = candidate;
+            break;
+        }
+    }
 
-    // We need to cache all services, characteristics and descriptors in the class, else
-    // the underlying objects will be garbage collected.
-    auto services_result = async_get(device_.GetGattServicesAsync(BluetoothCacheMode::Uncached));
-    if (services_result.Status() != GattCommunicationStatus::Success) {
+    GattDeviceServicesResult services_result{nullptr};
+    if (!requested_service.empty()) {
+        services_result = async_get(device_.GetGattServicesForUuidAsync(
+            uuid_to_guid(requested_service), BluetoothCacheMode::Cached), 30000);
+        if (services_result.Status() != GattCommunicationStatus::Success || services_result.Services().Size() == 0) {
+            services_result = async_get(device_.GetGattServicesForUuidAsync(
+                uuid_to_guid(requested_service), BluetoothCacheMode::Uncached), 30000);
+        }
+    } else {
+        services_result = async_get(device_.GetGattServicesAsync(BluetoothCacheMode::Cached), 30000);
+        if (services_result.Status() != GattCommunicationStatus::Success || services_result.Services().Size() == 0) {
+            services_result = async_get(device_.GetGattServicesAsync(BluetoothCacheMode::Uncached), 30000);
+        }
+    }
+    if (services_result.Status() != GattCommunicationStatus::Success || services_result.Services().Size() == 0) {
         return false;
     }
 
     auto gatt_services = services_result.Services();
     for (GattDeviceService&& service : gatt_services) {
-        // For each service...
-        gatt_service_t gatt_service;
-        gatt_service.obj = service;
-
-        // Save the MTU size
-        mtu_ = service.Session().MaxPduSize();
-
-        // Fetch the service UUID
-        std::string service_uuid = guid_to_uuid(service.Uuid());
-
-        // Fetch the service characteristics
-        auto characteristics_result = async_get(service.GetCharacteristicsAsync(BluetoothCacheMode::Uncached));
-        if (characteristics_result.Status() != GattCommunicationStatus::Success) {
-            return false;
-        }
-
-        // Load the characteristics into the service
-        auto gatt_characteristics = characteristics_result.Characteristics();
-        for (GattCharacteristic&& characteristic : gatt_characteristics) {
-            // For each characteristic...
-            gatt_characteristic_t gatt_characteristic;
-            gatt_characteristic.obj = characteristic;
-
-            // Fetch the characteristic UUID
-            std::string characteristic_uuid = guid_to_uuid(characteristic.Uuid());
-
-            // Fetch the characteristic descriptors
-            auto descriptors_result = async_get(characteristic.GetDescriptorsAsync(BluetoothCacheMode::Uncached));
-            if (descriptors_result.Status() != GattCommunicationStatus::Success) {
-                return false;
+        const std::string service_uuid = guid_to_uuid(service.Uuid());
+        GattSession session{nullptr};
+        try {
+            gatt_service_t gatt_service;
+            gatt_service.obj = service;
+
+            try {
+                session = service.Session();
+                session.MaintainConnection(true);
+                mtu_ = session.MaxPduSize();
+            } catch (...) {
+                // MaintainConnection is an optimization and must not invalidate an
+                // otherwise usable PB-GATT service.
             }
 
-            // Load the descriptors into the characteristic
-            auto gatt_descriptors = descriptors_result.Descriptors();
-            for (GattDescriptor&& descriptor : gatt_descriptors) {
-                // For each descriptor...
-                gatt_descriptor_t gatt_descriptor;
-                gatt_descriptor.obj = descriptor;
-
-                // Fetch the descriptor UUID.
-                std::string descriptor_uuid = guid_to_uuid(descriptor.Uuid());
-
-                // Append the descriptor to the characteristic.
-                gatt_characteristic.descriptors.emplace(descriptor_uuid, std::move(gatt_descriptor));
+            auto characteristics_result = async_get(
+                service.GetCharacteristicsAsync(BluetoothCacheMode::Cached), 30000);
+            if (characteristics_result.Status() != GattCommunicationStatus::Success ||
+                characteristics_result.Characteristics().Size() == 0) {
+                characteristics_result = async_get(
+                    service.GetCharacteristicsAsync(BluetoothCacheMode::Uncached), 30000);
+            }
+            if (characteristics_result.Status() != GattCommunicationStatus::Success ||
+                characteristics_result.Characteristics().Size() == 0) {
+                throw SimpleBLE::Exception::OperationFailed(
+                    "GATT characteristic discovery failed for service " + service_uuid);
             }
 
-            // Append the characteristic to the service.
-            gatt_service.characteristics.emplace(characteristic_uuid, std::move(gatt_characteristic));
+            for (GattCharacteristic&& characteristic : characteristics_result.Characteristics()) {
+                gatt_characteristic_t gatt_characteristic;
+                gatt_characteristic.obj = characteristic;
+                const std::string characteristic_uuid = guid_to_uuid(characteristic.Uuid());
+
+                // PB-GATT does not need explicit descriptor enumeration: WinRT's
+                // notification API writes the CCCD itself. Keep descriptor support
+                // for non-Mesh services as a best-effort compatibility path.
+                if (service_uuid != kProvisioningService && service_uuid != kProxyService) {
+                    try {
+                        auto descriptors_result = async_get(
+                            characteristic.GetDescriptorsAsync(BluetoothCacheMode::Cached));
+                        if (descriptors_result.Status() == GattCommunicationStatus::Success) {
+                            for (GattDescriptor&& descriptor : descriptors_result.Descriptors()) {
+                                gatt_descriptor_t gatt_descriptor;
+                                gatt_descriptor.obj = descriptor;
+                                gatt_characteristic.descriptors.emplace(
+                                    guid_to_uuid(descriptor.Uuid()), std::move(gatt_descriptor));
+                            }
+                        }
+                    } catch (...) {
+                    }
+                }
+                gatt_service.characteristics.emplace(characteristic_uuid, std::move(gatt_characteristic));
+            }
+            if (!gatt_service.characteristics.empty()) {
+                gatt_map_.emplace(service_uuid, std::move(gatt_service));
+            }
+        } catch (...) {
+            if (session != nullptr) {
+                try {
+                    session.MaintainConnection(false);
+                } catch (...) {
+                }
+            }
+            try {
+                service.Close();
+            } catch (...) {
+            }
+            if (service_uuid == requested_service) throw;
         }
-
-        // Append the service to the map.
-        gatt_map_.emplace(service_uuid, std::move(gatt_service));
     }
 
-    return true;
+    return requested_service.empty() ? !gatt_map_.empty() : gatt_map_.count(requested_service) != 0;
 }
 
 gatt_characteristic_t& PeripheralBase::_fetch_characteristic(const BluetoothUUID& service_uuid,
diff --git a/simpleble/src/backends/windows/PeripheralBase.h b/simpleble/src/backends/windows/PeripheralBase.h
index 030ce84..b81549f 100644
--- a/simpleble/src/backends/windows/PeripheralBase.h
+++ b/simpleble/src/backends/windows/PeripheralBase.h
@@ -110,6 +110,7 @@ class PeripheralBase {
     std::map<BluetoothUUID, SimpleBLE::ByteArray> service_data_;
 
     bool _attempt_connect();
+    void _cleanup_gatt() noexcept;
 
     gatt_characteristic_t& _fetch_characteristic(const BluetoothUUID& service_uuid,
                                                  const BluetoothUUID& characteristic_uuid);
diff --git a/simpleble/src/backends/windows/Utils.h b/simpleble/src/backends/windows/Utils.h
index 770cc7b..911245b 100644
--- a/simpleble/src/backends/windows/Utils.h
+++ b/simpleble/src/backends/windows/Utils.h
@@ -29,14 +29,21 @@ ByteArray ibuffer_to_bytearray(const IBuffer& buffer);
 IBuffer bytearray_to_ibuffer(const ByteArray& array);
 
 template <typename async_t>
-static auto async_get(async_t const& async) {
-    if (async.Status() == Foundation::AsyncStatus::Started) {
-        wait_for_completed(async, TEN_SECONDS_IN_MSECS);
-    }
+static auto async_get(async_t const& async, uint32_t timeout_ms = TEN_SECONDS_IN_MSECS) {
     try {
+        if (async.Status() == Foundation::AsyncStatus::Started) {
+            wait_for_completed(async, timeout_ms);
+        }
+        if (async.Status() == Foundation::AsyncStatus::Started) {
+            async.Cancel();
+            throw SimpleBLE::Exception::OperationFailed(
+                "WinRT operation still pending after " + std::to_string(timeout_ms) + " ms");
+        }
         return async.GetResults();
     } catch (const winrt::hresult_error& err) {
         throw SimpleBLE::Exception::WinRTException(err.code().value, winrt::to_string(err.message()));
+    } catch (const SimpleBLE::Exception::BaseException&) {
+        throw;
     } catch (const std::exception& err) {
         throw SimpleBLE::Exception::BaseException(err.what());
     }
diff --git a/simpleble/src/backends/windows/AdapterBase.cpp b/simpleble/src/backends/windows/AdapterBase.cpp
index 7bd3951..548a04e 100644
--- a/simpleble/src/backends/windows/AdapterBase.cpp
+++ b/simpleble/src/backends/windows/AdapterBase.cpp
@@ -290,7 +290,10 @@ void AdapterBase::_scan_received_callback(advertising_data_t data) {
         // Store it in our table of seen peripherals
         this->seen_peripherals_.insert(std::make_pair(data.mac_address, base_peripheral));
         SAFE_CALLBACK_CALL(this->callback_on_scan_found_, peripheral_builder);
-    } else {
-        SAFE_CALLBACK_CALL(this->callback_on_scan_updated_, peripheral_builder);
     }
+
+    // Always emit the merged advertisement snapshot through the update callback,
+    // including the first packet. Klixa consumes this single stream because BLE
+    // Mesh advertisements are frequently split across successive PDUs.
+    SAFE_CALLBACK_CALL(this->callback_on_scan_updated_, peripheral_builder);
 }
diff --git a/simpleble/src_c/peripheral.cpp b/simpleble/src_c/peripheral.cpp
index dcd8fb1..67a14f7 100644
--- a/simpleble/src_c/peripheral.cpp
+++ b/simpleble/src_c/peripheral.cpp
@@ -2,6 +2,7 @@
 
 #include <simpleble/PeripheralSafe.h>
 
+#include <algorithm>
 #include <climits>
 #include <cstring>
 #include <map>
@@ -176,8 +177,8 @@ simpleble_err_t simpleble_peripheral_services_get(simpleble_peripheral_t handle,
 
     memcpy(services->uuid.value, service.uuid().c_str(), SIMPLEBLE_UUID_STR_LEN);
 
-    services->data_length = service.data().size();
-    memcpy(services->data, service.data().data(), service.data().size());
+    services->data_length = std::min(service.data().size(), sizeof(services->data));
+    memcpy(services->data, service.data().data(), services->data_length);
 
     services->characteristic_count = service.characteristics().size();
     if (services->characteristic_count > SIMPLEBLE_CHARACTERISTIC_MAX_COUNT) {
@@ -251,8 +252,9 @@ simpleble_err_t simpleble_peripheral_manufacturer_data_get(simpleble_peripheral_
 
     auto& selected_manufacturer_data = *it;
     manufacturer_data->manufacturer_id = selected_manufacturer_data.first;
-    manufacturer_data->data_length = selected_manufacturer_data.second.size();
-    memcpy(manufacturer_data->data, selected_manufacturer_data.second.data(), selected_manufacturer_data.second.size());
+    manufacturer_data->data_length =
+        std::min(selected_manufacturer_data.second.size(), sizeof(manufacturer_data->data));
+    memcpy(manufacturer_data->data, selected_manufacturer_data.second.data(), manufacturer_data->data_length);
 
     return SIMPLEBLE_SUCCESS;
 }
@@ -414,11 +416,14 @@ simpleble_err_t simpleble_peripheral_write_descriptor(simpleble_peripheral_t han
 simpleble_err_t simpleble_peripheral_set_callback_on_connected(simpleble_peripheral_t handle,
                                                                void (*callback)(simpleble_peripheral_t, void*),
                                                                void* userdata) {
-    if (handle == nullptr || callback == nullptr) {
+    if (handle == nullptr) {
         return SIMPLEBLE_FAILURE;
     }
 
     SimpleBLE::Safe::Peripheral* peripheral = (SimpleBLE::Safe::Peripheral*)handle;
+    if (callback == nullptr) {
+        return peripheral->set_callback_on_connected(nullptr) ? SIMPLEBLE_SUCCESS : SIMPLEBLE_FAILURE;
+    }
 
     bool success = peripheral->set_callback_on_connected([=]() { callback(handle, userdata); });
 
@@ -428,11 +433,14 @@ simpleble_err_t simpleble_peripheral_set_callback_on_connected(simpleble_periphe
 simpleble_err_t simpleble_peripheral_set_callback_on_disconnected(simpleble_peripheral_t handle,
                                                                   void (*callback)(simpleble_peripheral_t, void*),
                                                                   void* userdata) {
-    if (handle == nullptr || callback == nullptr) {
+    if (handle == nullptr) {
         return SIMPLEBLE_FAILURE;
     }
 
     SimpleBLE::Safe::Peripheral* peripheral = (SimpleBLE::Safe::Peripheral*)handle;
+    if (callback == nullptr) {
+        return peripheral->set_callback_on_disconnected(nullptr) ? SIMPLEBLE_SUCCESS : SIMPLEBLE_FAILURE;
+    }
 
     bool success = peripheral->set_callback_on_disconnected([=]() { callback(handle, userdata); });
 
diff --git a/simpleble/src_c/adapter.cpp b/simpleble/src_c/adapter.cpp
index 878050a..801799c 100644
--- a/simpleble/src_c/adapter.cpp
+++ b/simpleble/src_c/adapter.cpp
@@ -153,6 +153,9 @@ simpleble_err_t simpleble_adapter_set_callback_on_scan_start(simpleble_adapter_t
     }
 
     SimpleBLE::Safe::Adapter* adapter = (SimpleBLE::Safe::Adapter*)handle;
+    if (callback == nullptr) {
+        return adapter->set_callback_on_scan_start(nullptr) ? SIMPLEBLE_SUCCESS : SIMPLEBLE_FAILURE;
+    }
 
     bool success = adapter->set_callback_on_scan_start([=]() { callback(handle, userdata); });
     return success ? SIMPLEBLE_SUCCESS : SIMPLEBLE_FAILURE;
@@ -166,6 +169,9 @@ simpleble_err_t simpleble_adapter_set_callback_on_scan_stop(simpleble_adapter_t
     }
 
     SimpleBLE::Safe::Adapter* adapter = (SimpleBLE::Safe::Adapter*)handle;
+    if (callback == nullptr) {
+        return adapter->set_callback_on_scan_stop(nullptr) ? SIMPLEBLE_SUCCESS : SIMPLEBLE_FAILURE;
+    }
 
     bool success = adapter->set_callback_on_scan_stop([=]() { callback(handle, userdata); });
     return success ? SIMPLEBLE_SUCCESS : SIMPLEBLE_FAILURE;
@@ -178,6 +184,9 @@ simpleble_err_t simpleble_adapter_set_callback_on_scan_updated(
     }
 
     SimpleBLE::Safe::Adapter* adapter = (SimpleBLE::Safe::Adapter*)handle;
+    if (callback == nullptr) {
+        return adapter->set_callback_on_scan_updated(nullptr) ? SIMPLEBLE_SUCCESS : SIMPLEBLE_FAILURE;
+    }
 
     bool success = adapter->set_callback_on_scan_updated([=](SimpleBLE::Safe::Peripheral peripheral) {
         // Create a peripheral handle
@@ -194,6 +203,9 @@ simpleble_err_t simpleble_adapter_set_callback_on_scan_found(
     }
 
     SimpleBLE::Safe::Adapter* adapter = (SimpleBLE::Safe::Adapter*)handle;
+    if (callback == nullptr) {
+        return adapter->set_callback_on_scan_found(nullptr) ? SIMPLEBLE_SUCCESS : SIMPLEBLE_FAILURE;
+    }
 
     bool success = adapter->set_callback_on_scan_found([=](SimpleBLE::Safe::Peripheral peripheral) {
         // Create a peripheral handle
diff --git a/simpleble/CMakeLists.txt b/simpleble/CMakeLists.txt
index d983902..8124c24 100644
--- a/simpleble/CMakeLists.txt
+++ b/simpleble/CMakeLists.txt
@@ -1,4 +1,4 @@
-cmake_minimum_required(VERSION 3.21)
+cmake_minimum_required(VERSION 3.20)
 
 include(${CMAKE_CURRENT_SOURCE_DIR}/../cmake/prelude.cmake)
 
diff --git a/cmake/prelude.cmake b/cmake/prelude.cmake
index f17b0a5..fe4fcc0 100644
--- a/cmake/prelude.cmake
+++ b/cmake/prelude.cmake
@@ -1,4 +1,4 @@
-cmake_minimum_required(VERSION 3.21)
+cmake_minimum_required(VERSION 3.20)
 
 # Common configuration used by all projects in this repository
 set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
